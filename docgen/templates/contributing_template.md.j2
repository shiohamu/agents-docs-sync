# Contributing to {{ project_name }}

このプロジェクトに貢献していただき、ありがとうございます！

## 開発環境のセットアップ

### 前提条件

{% if has_python %}
- Python 3.8以上
{% endif %}
{% if has_javascript %}
- Node.js 16以上
{% endif %}
{% if has_go %}
- Go 1.19以上
{% endif %}

### インストール

1. リポジトリをクローン:
```bash
git clone https://github.com/yourusername/{{ project_name }}.git
cd {{ project_name }}
```

{% if has_python %}
2. Python依存関係のインストール:
```bash
{% if python_pm == "uv" %}
uv sync
{% elif python_pm == "poetry" %}
poetry install
{% elif python_pm == "pipenv" %}
pipenv install --dev
{% else %}
pip install -r requirements.txt
{% endif %}
```
{% endif %}

{% if has_javascript %}
2. Node.js依存関係のインストール:
```bash
{% if js_pm == "pnpm" %}
pnpm install
{% elif js_pm == "yarn" %}
yarn install
{% elif js_pm == "bun" %}
bun install
{% else %}
npm install
{% endif %}
```
{% endif %}

## コーディング規約

{% if coding_standards %}
このプロジェクトでは以下のツールを使用してコード品質を保っています:

{% if linter %}
- **Linter**: {{ linter }}
{% endif %}
{% if formatter %}
- **Formatter**: {{ formatter }}
{% endif %}

コミット前に以下を実行してください:
```bash
{% if linter == "ruff" %}
ruff check . --fix
ruff format .
{% elif linter == "eslint" %}
{% if js_pm == "pnpm" %}
pnpm run lint
{% elif js_pm == "yarn" %}
yarn lint
{% else %}
npm run lint
{% endif %}
{% endif %}
```
{% else %}
プロジェクト固有のコーディング規約を遵守してください。
{% endif %}

## テストの実行

変更を加えた後は、必ずテストを実行してください:

{% if test_commands %}
{% for cmd in test_commands %}
```bash
{{ cmd }}
```
{% endfor %}
{% else %}
```bash
# テストコマンドは自動検出されませんでした
# プロジェクト固有のテスト方法に従ってください
```
{% endif %}

## プルリクエストのプロセス

1. 新しいブランチを作成:
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. 変更を加えてコミット:
   ```bash
   git add .
   git commit -m "Add: your feature description"
   ```

3. ブランチをプッシュ:
   ```bash
   git push origin feature/your-feature-name
   ```

4. GitHubでプルリクエストを作成

5. レビューを待ち、必要に応じて修正

## コミットメッセージの規約

明確なコミットメッセージを書いてください:

- `Add:` 新機能の追加
- `Fix:` バグ修正
- `Update:` 既存機能の更新
- `Refactor:` リファクタリング
- `Docs:` ドキュメントの変更
- `Test:` テストの追加・修正

例:
```
Add: user authentication feature
Fix: resolve login timeout issue
Update: improve error handling in API client
```

## 質問やサポート

問題が発生した場合は、GitHubのIssuesで質問してください。

---

{{ footer }}
