#!/bin/bash
#
# post-commitフック
# コミット後にドキュメントを更新（オプション）
# 注意: このフックは別のコミットとしてドキュメント更新を追加します
#
# このフックは現在無効化されています
# 有効化する場合は、環境変数 DOCGEN_ENABLE_POST_COMMIT=1 を設定してください
#

# post-commitフックを無効化（デフォルトで無効）
# 有効化する場合は、以下の行をコメントアウトし、環境変数を設定してください
# デバッグ用: フックが呼ばれたことを確認する場合は、以下のコメントを外してください
# echo "[DEBUG] post-commitフックが呼ばれましたが、無効化されているため終了します" >&2
exit 0
